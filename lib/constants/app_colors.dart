import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primaryPurple = Color(0xFF7C3AED);
  static const Color primaryDark = Color(0xFF0F172A);
  static const Color primaryDarkSecondary = Color(0xFF1E293B);

  // Text Colors
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textGrey = Color(0xFFE2E8F0);
  static const Color textGreySecondary = Color(0xFFA0AEC0);

  // Gradients and overlays
  static const List<Color> darkGradient = [
    Color(0x00000000),
    Color(0x4D000000),
    Color(0xFF0F172A),
  ];

  // Page indicator
  static const Color indicatorInactive = Color(0xFF64748B);
  static const Color indicatorActive = Color(0xFF7C3AED);

  // Transparent
  static const Color transparent = Color(0x00000000);
}
